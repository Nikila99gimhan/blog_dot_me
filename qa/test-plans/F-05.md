# F-05 — Blog Post Rendering (Markdown → HTML)

**Priority:** P0
**Source:** quartz.config.ts (transformers), content/devops/kubernetes-zero-to-prod.md (agent-inferred)

---

## Happy Path

### F-05-TC-1: Post page loads with correct title and metadata

**Steps:**
1. Navigate to `http://localhost:8080/devops/kubernetes-zero-to-prod`

**Expected:**
- Page renders without error
- Article title "Kubernetes: Zero to Prod" is visible as `<h1>`
- Date `2026-06-20` is displayed (ContentMeta component)
- Tags [kubernetes, devops, containers, k8s] are displayed as clickable tag pills

**Screenshot checkpoint:** post header with title, date, tags

---

### F-05-TC-2: Markdown headings render as correct HTML heading levels

**Steps:**
1. Navigate to `/devops/kubernetes-zero-to-prod`
2. Scroll through the article

**Expected:**
- `## Step 1 — Start a local cluster` renders as `<h2>`
- `## Step 2 — Write your first Deployment` renders as `<h2>`
- Heading hierarchy is logical and consistent
- Headings have anchor IDs (for TOC linking)

**Screenshot checkpoint:** multiple headings visible

---

### F-05-TC-3: Code blocks render with correct language labels

**Steps:**
1. Navigate to `/devops/kubernetes-zero-to-prod`
2. Inspect code blocks

**Expected:**
- ` ```bash ` blocks render with "bash" language indicator
- ` ```yaml ` blocks render with "yaml" language indicator
- Code content is preserved exactly (no escaping issues)
- Code blocks are scrollable if content overflows width

**Screenshot checkpoint:** bash code block, yaml code block

---

### F-05-TC-4: GFM tables render correctly

**Steps:**
1. Navigate to `/devops/github-actions-reusable-workflows`
2. Find the "Tips & Gotchas" table

**Expected:**
- Table renders with proper columns: "Gotcha" | "Fix"
- All 4 rows are present
- Table is responsive (scrollable on narrow viewport)

**Screenshot checkpoint:** table rendered

---

### F-05-TC-5: Callout blocks render with correct styling

**Steps:**
1. Navigate to `/devops/kubernetes-zero-to-prod`
2. Find the `> [!tip]` callout

**Expected:**
- Callout renders with a distinct style (colored border, icon, or background)
- Text "Never hardcode config in your container image..." is visible
- Callout is clearly distinguished from normal blockquotes

**Screenshot checkpoint:** [!tip] callout rendered

---

### F-05-TC-6: Second post renders correctly

**Steps:**
1. Navigate to `/devops/github-actions-reusable-workflows`

**Expected:**
- Title "GitHub Actions Reusable Workflows" as h1
- Date `2026-06-15` in ContentMeta
- Tags [github-actions, cicd, devops, automation] visible
- `> [!warning]` callout renders distinctly from `[!tip]`

**Screenshot checkpoint:** second post rendered

---

## Edge Cases

### F-05-TC-7: Post renders on mobile without overflow

**Steps:**
1. Set viewport to 375×812
2. Navigate to `/devops/kubernetes-zero-to-prod`

**Expected:**
- No horizontal scroll on main content area
- Code blocks have horizontal scroll if content is wide (not forced wrapping)
- Fonts are readable at mobile size

**Screenshot checkpoint:** mobile post view

---

## Negative Cases

### F-05-TC-8: Navigating to a non-existent post shows 404, not a crash

**Steps:**
1. Navigate to `/devops/this-post-does-not-exist`

**Expected:**
- Styled 404 page is shown (see F-15)
- No unhandled JavaScript error
- Site remains functional after viewing the 404 page

---

## Regression Checks
_No prior run baseline. First execution._

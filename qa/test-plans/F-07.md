# F-07 — Table of Contents

**Priority:** P1
**Source:** quartz.config.ts (Plugin.TableOfContents collapseByDefault: false, minEntries: 2), quartz.layout.ts line 34 (agent-inferred)

---

## Happy Path

### F-07-TC-1: TOC is visible and expanded on a multi-section post

**Steps:**
1. Desktop viewport (1440×900)
2. Navigate to `/devops/kubernetes-zero-to-prod`

**Expected:**
- Table of Contents is visible in the right sidebar
- TOC is expanded by default (`collapseByDefault: false`)
- TOC lists all major headings from the post (Step 1 through Step 7, Cheatsheet, What's Next)
- Each TOC entry is a clickable link

**Screenshot checkpoint:** right sidebar with TOC fully visible and expanded

---

### F-07-TC-2: Clicking a TOC entry scrolls to the correct heading

**Steps:**
1. Desktop viewport, navigate to `/devops/kubernetes-zero-to-prod`
2. Click "Step 5 — ConfigMaps & Secrets" in the TOC

**Expected:**
- Page scrolls to the "Step 5" heading
- Heading is visible and near the top of the viewport (with appropriate offset)
- URL hash updates to `#step-5--configmaps--secrets` (or similar slugified anchor)

**Screenshot checkpoint:** after scroll, heading 5 visible in viewport

---

### F-07-TC-3: Active TOC entry updates as user scrolls

**Steps:**
1. Desktop viewport, navigate to `/devops/kubernetes-zero-to-prod`
2. Slowly scroll down through the article

**Expected:**
- As each heading enters the viewport, its corresponding TOC entry becomes highlighted/active
- Active state is visually distinct (different color, font weight, or indicator)
- Only one TOC entry is active at a time

**Screenshot checkpoints:** TOC with entry 1 active, TOC with entry 4 active

---

## Edge Cases

### F-07-TC-4: TOC is hidden on the homepage (no headings meeting minEntries)

**Steps:**
1. Desktop viewport, navigate to `http://localhost:8080/`

**Expected:**
- TOC panel is NOT shown in the right sidebar
- Right sidebar is empty or not rendered
- No empty TOC container is left as a visual artifact

**Screenshot checkpoint:** homepage right sidebar (should be empty)

---

### F-07-TC-5: TOC is not shown on mobile viewport

**Steps:**
1. Set viewport to 375×812
2. Navigate to `/devops/kubernetes-zero-to-prod`

**Expected:**
- TOC panel is NOT visible (it is `DesktopOnly`)
- Post content takes full width
- No empty space where TOC would be

**Screenshot checkpoint:** mobile post view — no TOC

---

### F-07-TC-6: TOC correctly represents heading hierarchy (h2 vs h3)

**Steps:**
1. Desktop viewport, open a post with nested headings (h2 and h3)

**Expected:**
- h3 entries are indented relative to their parent h2 in the TOC
- Hierarchy is visually clear

---

## Negative Cases

### F-07-TC-7: TOC not shown for posts with fewer than 2 headings

**Steps:**
1. If a short post with only one heading (or none) exists, navigate to it

**Expected:**
- TOC panel does not appear (`minEntries: 2` configured)
- Right sidebar is clean with no empty TOC

**Note:** Verify this with content/projects/index.md if it has < 2 headings.

---

## Regression Checks
_No prior run baseline. First execution._

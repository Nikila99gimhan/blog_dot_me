# F-16 — Wikilinks Resolution

**Priority:** P1
**Source:** quartz.config.ts (Plugin.CrawlLinks markdownLinkResolution: shortest), content/devops/kubernetes-zero-to-prod.md line 214 (agent-inferred)

---

## Happy Path

### F-16-TC-1: Wikilink with alias text renders as correct hyperlink

**Steps:**
1. Navigate to `/devops/kubernetes-zero-to-prod`
2. Scroll to "What's Next?" section

**Expected:**
- `[[devops/github-actions-reusable-workflows|CI/CD with GitHub Actions]]` renders as:
  - Link text: "CI/CD with GitHub Actions" (the alias)
  - `href`: `/devops/github-actions-reusable-workflows`
- Link is styled as a standard internal link (underline, site accent color)

**Screenshot checkpoint:** "What's Next?" section with visible wikilink

---

### F-16-TC-2: Clicking a wikilink navigates to the correct target page

**Steps:**
1. Click "CI/CD with GitHub Actions" link in the kubernetes post

**Expected:**
- Page navigates to `/devops/github-actions-reusable-workflows`
- URL updates correctly
- Page title matches "GitHub Actions Reusable Workflows"

**Screenshot checkpoint:** GitHub Actions post loaded after wikilink click

---

### F-16-TC-3: Shortest path resolution works (unqualified wikilinks)

**Steps:**
1. In `github-actions-reusable-workflows.md`, `[[devops/kubernetes-zero-to-prod|Kubernetes: Zero to Prod]]` uses a path-qualified wikilink
2. Navigate to that post and verify the link renders

**Expected:**
- Link resolves correctly regardless of path prefix
- Link text "Kubernetes: Zero to Prod" is shown
- `href` points to `/devops/kubernetes-zero-to-prod`

**Screenshot checkpoint:** related posts section link in github-actions post

---

### F-16-TC-4: Wikilinks in homepage render correctly

**Steps:**
1. Navigate to `http://localhost:8080/`
2. In "Recent Posts" section, verify wikilinks like `[[devops/kubernetes-zero-to-prod|...]]`

**Expected:**
- Each recent post entry is a clickable link
- Links resolve to correct target pages

**Screenshot checkpoint:** homepage recent posts section with clickable links

---

## Edge Cases

### F-16-TC-5: Wikilink without alias uses filename as text

**Steps:**
1. Find a `[[pagename]]` wikilink without an alias (if any exists in content)
2. Verify the rendered link text

**Expected:**
- Link text is the page title (from frontmatter) or filename (if no title)
- Link resolves correctly

---

### F-16-TC-6: Broken wikilink (target page does not exist) is visually distinct

**Steps:**
1. Identify any wikilink in content that points to a page that doesn't exist
2. Inspect its rendered state

**Expected:**
- Broken links are styled differently (grey, strikethrough, or distinct color)
- Broken link does NOT navigate to a 404 without visual warning
- Quartz's default behavior for broken wikilinks is applied

---

## Negative Cases

### F-16-TC-7: Wikilinks do not create circular redirect loops

**Steps:**
1. Open devtools console
2. Navigate between two mutually linked posts (kubernetes → github-actions → kubernetes)
3. Repeat the cycle 3 times

**Expected:**
- Navigation works correctly each time
- No infinite redirect detected
- No console errors from circular link traversal

---

## Regression Checks
_No prior run baseline. First execution._
